require 'spec_helper'

describe RspecMemoryFormatter do
  it 'has a version number' do
    expect(RspecMemoryFormatter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
